# /handle-feedback

Read `.claude/skills/handle-feedback/SKILL.md` and execute it.

This is a standalone phase — it does not go through the controller. It reads
`artifacts/pr-log.md` directly and operates without prior workflow context.

$ARGUMENTS
